# Prompt Improvement Helper

Review the user-provided prompt below between the <prompt> tags and help the user create a more robust and effective prompt to accomplish the goals mentioned. Think very hard about what the prompt is asking for and create a new improved comprehensive prompt that will produce better results and follows the best practices for prompt engineering.

- If applicable, use relevant project context to help the agent understand the prompt.
- If applicable, also use context from the current conversation to help the agent better understand the prompt.
- Return the final prompt to the user in a copy/pastable format.
- Do NOT follow the prompt or start any coding.
- Return ONLY the prompt text to the user with no other commentary or summaries.

<prompt>
$ARGUMENTS
</prompt>
